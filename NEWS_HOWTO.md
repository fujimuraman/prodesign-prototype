# NEWS の追加・更新のしかた（プロデザイン公式サイト）

ニュースは **`news/` フォルダにテキストファイルを1つ置くだけ** で、一覧ページ・記事ページ・トップのニュース欄に自動で反映されます。
HTML を触る必要はありません。

## 1. 記事ファイルを作る

ファイル名: `news/2026-09-16-任意の名前.md`（日付は西暦-月-日。半角。名前は英数字が無難）

中身の例:

```
---
title: 令和8年 9月 大仙市メーカー様に箔研磨装置を納入しました
date: 2026.09.16
category: 実績
image: 2026-09-16-foil.jpg
---

大仙市の医療機器メーカー様に箔研磨装置を納入しました。

●納入先　大仙市 医療機器メーカー様
●内容　　手作業で行っていた箔の研磨工程を装置化

装置の詳細はお問い合わせください。
```

- `title`（見出し）と `date`（日付）は必須。`category` は「お知らせ／実績／専門家支援／セミナー／メディア」のどれか（無ければ「お知らせ」）
- `image` は写真のファイル名。写真は `images/news/` フォルダに入れる（無ければ行ごと消してOK）
- `---` から下が本文。空行で段落が分かれる。「●」や「・」で始まる行は箇条書きになる
- 本文に写真を追加したい時は `![](images/news/写真名.jpg)` と1行書く

## 2. GitHub に置く（ブラウザだけで完結）

1. https://github.com/fujimuraman/prodesign-prototype を開く（ログインが必要。アカウントは薫が招待します）
2. `news` フォルダを開き、右上の **Add file → Create new file**
3. ファイル名欄に `2026-09-16-foil.md`、本文欄に上の内容を貼り付け
4. 下の **Commit changes** を押す
5. 写真がある場合は `images/news` フォルダで **Add file → Upload files** から写真をアップロードして Commit

数分後に自動で公開サイトへ反映されます（GitHub Actions が `build_news.py` を実行して news.html などを作り直します）。

## 3. 直したい・消したい時

- 直す: 同じファイルを開いて鉛筆マーク（Edit）→ 修正 → Commit
- 消す: ファイルを開いて「…」→ Delete file → Commit

## 4. 手元のPCで確認したい時（任意）

```
python build_news.py
```

を実行すると、その場で news.html / post-*.html / index.html が更新されます。

## 5. 困ったら

薫（fujimuraman@gmail.com）にメールで「タイトル・日付・本文・写真」を送ってもらえれば、こちらで登録します。
