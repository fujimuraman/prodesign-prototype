# ニュース・事業実績の更新のしかた（プロデザイン公式サイト）

ブログのように、**更新ページ**から書き込むだけで公開されます。パソコン・スマホどちらでも使えます。

## 更新ページ

https://fujimuraman.github.io/prodesign-prototype/admin.html
（公開後は https://prodesign.co.jp/admin.html になります）

## 最初に一度だけ

1. 更新ページを開く
2. 右上の **設定** を押す
3. 薫から受け取った **アクセスキー**（`github_pat_…` で始まる文字列）を貼り付けて **保存**

アクセスキーはそのパソコン（スマホ）のブラウザに保存されます。別の端末で使う時はもう一度貼り付けてください。他の人には渡さないでください。

## ニュースを書く

1. **新しい記事を書く** タブ
2. タイトル・日付・分類（お知らせ／実績／専門家支援／セミナー／メディア）を入れる
3. 写真があれば **写真** で選ぶ（1枚・4MBまで）
4. 本文を入力（空行で段落、「●」や「・」で始まる行は箇条書き）。下にプレビューが出ます
5. **この内容で公開する** を押す

2〜3分で「ニュース一覧」「記事ページ」「トップページのニュース欄」に反映されます。

- 直す: **ニュース一覧** タブ → 記事の **編集** → 直して **公開**
- 消す: 編集画面の **この記事を削除**

## 沿革・事業実績を足す

1. **沿革・事業実績** タブ
2. **＋ 行を追加** → 年（例 `2026.09`）・和暦（例 `令和8年`）・内容（例 `箔研磨装置納入（大仙市メーカー様）`）
3. **この内容で公開する**

企業情報ページの時系列に反映されます。

## 仕組み（薫・管理者向け）

- 更新ページは GitHub API を直接叩き、`news/*.md`・`images/news/*`・`data/history.json` をコミットする（サーバー不要）
- コミットを検知して GitHub Actions（`.github/workflows/build-news.yml`）が `build_news.py` を実行し、`news.html`／`post-*.html`／`index.html` のニュース欄／`company.html` の事業実績を生成してコミット→GitHub Pages が再デプロイ
- アクセスキーは GitHub の **Fine-grained personal access token**（Repository access: prodesign-prototype のみ／Permissions: Contents = Read and write）。有効期限が切れたら作り直して渡す
- 手元で確認する時は `python build_news.py`
