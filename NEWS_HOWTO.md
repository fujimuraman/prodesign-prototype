# ニュース・事業実績の更新のしかた（プロデザイン公式サイト）

ブログのように、**更新ページ**にログインして書き込むだけで公開されます。パソコン・スマホどちらでも使えます。

## 更新ページ

https://prodesign.co.jp/admin （新サイト公開後に有効になります）
※ 今のプレビュー（https://fujimuraman.github.io/prodesign-prototype/admin.html）は見た目の確認用で、ログインはできません

## 最初に一度だけ（新規登録）

1. 更新ページを開き、「はじめての方（新規登録）」を押す
2. **メールアドレス**（会社のアドレス）と **パスワード**（8文字以上・英字と数字）を決めて登録
3. そのメールアドレスに **6桁の認証コード** が届くので、入力すると登録完了

登録できるメールアドレスはあらかじめ決めてあります（他の人は登録できません）。

## ログイン（2段階）

1. メールアドレスとパスワードを入力
2. メールに届く **6桁の認証コード** を入力
3. 「この端末を30日間記憶する」にチェックしておくと、同じパソコン・スマホでは30日間、認証コードの入力を省略できます

## ニュースを書く

1. **新しい記事を書く** タブ
2. タイトル・日付・分類（お知らせ／実績／専門家支援／セミナー／メディア）を入れる
3. 写真があれば **写真** で選ぶ（1枚・4MBまで）
4. 本文を入力（空行で段落、「●」や「・」で始まる行は箇条書き）。下にプレビューが出ます
5. **この内容で公開する** を押す → すぐにサイトに反映されます

- 直す: **ニュース一覧** → 記事の **編集** → 直して **公開**
- 消す: 編集画面の **この記事を削除**

## 沿革・事業実績を足す

**沿革・事業実績** タブ → **＋ 行を追加** → 年（例 `2026.09`）・和暦（例 `令和8年`）・内容 → **この内容で公開する**。企業情報ページの時系列に反映されます。

## パスワードを忘れたとき

ログイン画面の **パスワードを忘れた方** → メールアドレスを入力 → 届いた認証コードと新しいパスワードを入力。
（安全のため、パスワードそのものをメールで送ることはしません。認証コードで本人確認してから新しいパスワードを決めます）

## メールアドレスを変えるとき

ログイン後 **アカウント設定** → 新しいメールアドレスとパスワードを入力 → **新しいアドレス** に届いた認証コードを入力で変更完了。元のアドレスにも「変更しました」の通知が届きます。

## 安全のために

- 共用のパソコンでは、作業後に **ログアウト** してください
- パスワードを10回間違えると15分間ログインできなくなります（不正アクセス対策）
- 認証コードは10分で無効になります。届かない時は迷惑メールフォルダを確認してください

## 仕組み（管理者向け）

- 置き場所: さくらのレンタルサーバー（静的HTML＋PHP 8＋SQLite）。設置手順は `DEPLOY_SAKURA.md`
- データ: `data/prodesign.sqlite`（記事・沿革・アカウント・ログ）、画像は `uploads/`。メールは PHP の `mail()`（差出人 info@prodesign.co.jp）
- 認証: パスワードは bcrypt でハッシュ保存。ログインは毎回メール認証コード（信頼済み端末は30日省略）。セッションは HttpOnly Cookie（30日）。10回失敗で15分ロック
- 登録できるメールは `config.php` の `allowed_emails`。追加したい時はここに足す
- 記事ページは `/post/<slug>`（`templates/news_post.html` を雛形に `post.php` が生成）。旧 `post-xxx.html` のURLは自動で転送。一覧・トップ・企業情報は `page.php` が静的HTMLにDBの内容を差し込む
- GitHub Pages のプレビューは `php tools/export_static.php` で同じ見た目に書き出したもの（更新機能は動かない）
